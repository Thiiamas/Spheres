extends AimStrategy
class_name ScreenCenterAim

## FOLLOW camera: the mouse is captured for reactor aiming, so there's no useful
## cursor position. Aim through the screen centre (the crosshair you're looking
## along). Uses the base _screen_point (viewport centre) unchanged.
